# This rule is not used by the project iaas-controller itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to iaas.controllers.thalassa.cloud resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: iaas-controller
    app.kubernetes.io/managed-by: kustomize
  name: vpc-viewer-role
rules:
- apiGroups:
  - iaas.controllers.thalassa.cloud
  resources:
  - vpcs
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - iaas.controllers.thalassa.cloud
  resources:
  - vpcs/status
  verbs:
  - get
